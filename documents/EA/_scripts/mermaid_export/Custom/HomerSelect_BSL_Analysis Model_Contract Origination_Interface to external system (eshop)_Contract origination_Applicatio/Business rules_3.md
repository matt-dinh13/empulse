# Business rules

```mermaid
graph TD
    Specific_attribute_processing["Specific attribute processing"]
    Is_offer_unblock["Is offer unblock"]
    Creation_of_user["Creation of user"]
    Create_validation_error_message["Create validation error message"]
```
