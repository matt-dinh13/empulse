# DDM and DDS manipulations

```mermaid
graph TD
    Update_account_number_of_disbursement_account["Update account number of disbursement account"]
    Change_ECS_provider_on_DDM["Change ECS provider on DDM"]
```
